import React from 'react';

/**
 * NotificationInitializer is retained for backwards compatibility.
 * FCM initialization and listener setup are managed centrally by NotificationProvider.
 */
export function NotificationInitializer() {
  return null;
}
